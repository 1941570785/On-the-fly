diff --git a/poses/pose_initializer.py b/poses/pose_initializer.py
index dce739e..7974d32 100644
--- a/poses/pose_initializer.py
+++ b/poses/pose_initializer.py
@@ -993,9 +993,13 @@ class PoseInitializer():
         if not keyframes:
             return [], [], [], [], "no_candidate_refs"
         stat_by_kid = {int(s.get("ref_keyframe_id", -1)): s for s in ref_stats}
+
+        def ref_consensus_score(stat: dict[str, object]) -> float:
+            return float(stat.get("ref_consensus_score", stat.get("consensus_score", 0.0)) or 0.0)
+
         ranked = sorted(
             zip(keyframes, ref_stats),
-            key=lambda item: -float(item[1].get("consensus_score", 0.0)),
+            key=lambda item: -ref_consensus_score(item[1]),
         )
         selected: list[Keyframe] = []
         subset_trace: list[dict[str, object]] = []
diff --git a/scene/scene_model.py b/scene/scene_model.py
index e23ec38..e80e209 100644
--- a/scene/scene_model.py
+++ b/scene/scene_model.py
@@ -1022,6 +1022,11 @@ class SceneModel:
                         "candidate_is_support_eligible": bool(
                             keyframe.info.get("_paper_aligned_support_eligible_recovery_keyframe", False)
                         ),
+                        "candidate_is_bridge_reference": bool(
+                            keyframe.info.get("_paper_aligned_bridge_ref", False)
+                        ),
+                        "candidate_bridge_source_anchor": int(keyframe.info.get("_paper_aligned_bridge_source_anchor", -1) or -1),
+                        "candidate_bridge_support_only": bool(keyframe.info.get("_paper_aligned_bridge_support_only", False)),
                         "candidate_rank_before_filter": int(rank_by_index.get(int(index), 0)),
                         "candidate_rank_after_filter": int(
                             list(prev_keyframes_indices.tolist()).index(int(index)) + 1
diff --git a/tools/build_defer_recovery_support_bridge_v1.py b/tools/build_defer_recovery_support_bridge_v1.py
index c3b599c..6b2534e 100644
--- a/tools/build_defer_recovery_support_bridge_v1.py
+++ b/tools/build_defer_recovery_support_bridge_v1.py
@@ -303,9 +303,15 @@ def eval_gate(summary: dict[str, Any], v2221: dict[str, Any]) -> dict[str, Any]:
     density_ok = 25 <= dens <= 45
     rs = summary.get("recovery_support") or {}
     vrs = (v2221.get("recovery_support") or {}) if v2221 else {}
-    support_improved = (
-        (to_float(rs.get("valid_2d3d_median")) or 0) > (to_float(vrs.get("valid_2d3d_median")) or 0) * 1.5
-        or (to_float(rs.get("pnp_inliers_median")) or 0) > (to_float(vrs.get("pnp_inliers_median")) or 0) + 10
+    # Older V2_2_2_1 artifacts may not contain recovery_support_trace median fields.
+    # In that case, use the handoff reference values from the experiment brief
+    # (valid_2d3d ~= 246, PnP inliers ~= 3) instead of treating missing as zero.
+    v_valid_ref = to_float(vrs.get("valid_2d3d_median"), 246.0)
+    v_pnp_ref = to_float(vrs.get("pnp_inliers_median"), 3.0)
+    rs_valid = to_float(rs.get("valid_2d3d_median")) or 0.0
+    rs_pnp = to_float(rs.get("pnp_inliers_median")) or 0.0
+    support_improved = (rs_valid > float(v_valid_ref or 246.0) * 1.5) or (
+        rs_pnp > float(v_pnp_ref or 3.0) + 10.0
     )
     gap_improved = (summary.get("density_gap") or {}).get("gap_ge_11_count", 99) < (
         (v2221.get("density_gap") or {}).get("gap_ge_11_count", 0)
@@ -319,7 +325,9 @@ def eval_gate(summary: dict[str, Any], v2221: dict[str, Any]) -> dict[str, Any]:
         "recovery_support_improved_vs_v2221": support_improved,
         "gap_ge11_reduced_vs_v2221": gap_improved,
         "quality_not_worse_than_v2221": quality_ok,
-        "proceed_short1000": bool(safety_ok and density_ok and support_improved),
+        "support_reference_valid_2d3d_median": float(v_valid_ref or 246.0),
+        "support_reference_pnp_inliers_median": float(v_pnp_ref or 3.0),
+        "proceed_short1000": bool(safety_ok and density_ok and support_improved and gap_improved),
     }
 
 
@@ -380,6 +388,7 @@ def main() -> int:
     s800 = s1000 = None
     anchor_rows: list[dict[str, Any]] = []
     mat_rows: list[dict[str, Any]] = []
+    bridge_rows: list[dict[str, Any]] = []
 
     if args.run in {"short800", "all"}:
         rd = out / "support_bridge_short800"
@@ -392,7 +401,7 @@ def main() -> int:
         for r in trace.get("recovery_commit_materialization_events", []) or []:
             mat_rows.append({**r, "run": "short800"})
         for r in trace.get("anchor_transition_bridge_events", []) or []:
-            mat_rows.append({**dict(r), "run": "short800", "record_type": "bridge"})
+            bridge_rows.append({**dict(r), "run": "short800"})
 
     if args.run in {"short1000", "all"}:
         rd = out / "support_bridge_short1000"
@@ -405,8 +414,11 @@ def main() -> int:
             anchor_rows.extend({**r, "run": "short1000"} for r in anchor_metrics(trace))
             for r in trace.get("recovery_commit_materialization_events", []) or []:
                 mat_rows.append({**r, "run": "short1000"})
+            for r in trace.get("anchor_transition_bridge_events", []) or []:
+                bridge_rows.append({**dict(r), "run": "short1000"})
 
     write_csv(out / "anchor_rhythm_diagnostic.csv", anchor_rows)
+    write_csv(out / "anchor_transition_bridge_diagnostic.csv", bridge_rows)
     write_csv(out / "materialization_activation_diagnostic.csv", mat_rows)
     comp = build_comparison(s800 or {}, s1000 or {}, v800, v1000)
     write_csv(out / "support_bridge_comparison_vs_v2_2_2_1.csv", comp)
